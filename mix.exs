defmodule HostTriple.MixProject do
  use Mix.Project

  def project do
    [
      app: :host_triple,
      description: "A library that produces uniform host triples",
      version: "0.1.0",
      language: :erlang,
      deps: []
    ]
  end
end
